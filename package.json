{
  "name": "sheeesh",
  "version": "1.0.0",
  "private": true,
  "description": "sHeeSh: an etymology game. Words have relatives; the game is telling which is which. Builds to one HTML file.",
  "scripts": {
    "build": "node build.js",
    "test": "node --test test/"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "peerjs": "^1.5.4"
  }
}
